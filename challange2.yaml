- name: odd r even
  hosts: local
  connection: local
  vars:
    nums=10
 
  tasks:
  - name: even
    ansible.builtin.debug:
      msg: "{{nums}} is even"
    when: (nums | int) % 2

  - name: odd 
    ansible.builtin.debug:
      msg: "{{nums}} is even"
    when: (nums | int) % 2